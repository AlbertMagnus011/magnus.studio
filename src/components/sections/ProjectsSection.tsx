import { patterns } from "@/components/ui/patterns"



export const ProjectsSection = () => {
    return(
        <section
        className={`bg-[#f27362] h-screen ${patterns.dottedBg}`}>
            
        </section>
    )
}